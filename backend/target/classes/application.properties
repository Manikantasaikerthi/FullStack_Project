# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/tribal_marketplace?createDatabaseIfNotExist=true&useSSL=false}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:host}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Mani@81412241929}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Mail Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:manikantasaikearthi@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fdyhwxukuhogyasn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Settings
server.port=${PORT:8080}
auth.jwt.secret=${JWT_SECRET:your-very-secure-and-long-secret-key-that-is-at-least-32-characters}
auth.jwt.expiration=86400000

# Connection & Payload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-http-post-size=10MB
